# Base URL of the backend API (no trailing slash).
# In Docker Compose this is proxied through Nginx; in local `npm run dev`
# it points straight at the FastAPI dev server.
VITE_API_BASE_URL=http://localhost:8000
