import { PlaceholderPage } from '../components/PlaceholderPage'

export function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Store configuration: currency, negative-stock policy, return approval threshold."
      milestone="a later milestone"
    />
  )
}
