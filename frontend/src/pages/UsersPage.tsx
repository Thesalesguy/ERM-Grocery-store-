import { PlaceholderPage } from '../components/PlaceholderPage'

export function UsersPage() {
  return (
    <PlaceholderPage
      title="Users"
      description="User accounts and role assignment (RBAC)."
      milestone="a dedicated auth milestone, see M0 report"
    />
  )
}
